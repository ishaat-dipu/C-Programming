#include<stdio.h>
int main()
{
    // ! @  # % ^ & * ( ) { } [] 
    // - +  = \ |
    // _ $ -- they can be used for variable naming 
    float radius = 5;
    float pi= 3.1415;
    float area = pi*radius*radius;
    printf("%f", area);
}